name: "Fix: safe-area-ios-viewport"
description: >-
  Automated fix task created by Factory TPM.


  Original story: /home/bigmints/Projects/pi-app/.factory/stories/features/safe-area-ios-viewport.yaml

  Issue: CLI engineer is stuck in a file-enumeration loop: both delegations and the intervention stalled after listing
  338 files without making any changes. The agent appears unable to proceed with implementation - it keeps outputting
  the directory listing and never writes code or modifies files. This requires human debugging of the CLI agent's
  behavior. The story itself (safe area insets, 100dvh, viewport-fit=cover) is straightforward CSS/meta changes but the
  CLI cannot execute them.
status: done
feature:
  name: Fix
  slug: fix
target: {}
stack: {}
acceptance_criteria: []
fix_instructions: "CLI engineer is stuck in a file-enumeration loop: both delegations and the intervention stalled after
  listing 338 files without making any changes. The agent appears unable to proceed with implementation - it keeps
  outputting the directory listing and never writes code or modifies files. This requires human debugging of the CLI
  agent's behavior. The story itself (safe area insets, 100dvh, viewport-fit=cover) is straightforward CSS/meta changes
  but the CLI cannot execute them."
original_story: /home/bigmints/Projects/pi-app/.factory/stories/features/safe-area-ios-viewport.yaml
phase: 0
dependsOn: []
createdBy: factory-tpm
createdAt: 2026-05-26T12:13:20.909Z
